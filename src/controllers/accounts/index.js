const { CreateAccountController } = require('./create-account.controller');
const { LoginAccountController } = require('./login-account.controller');

module.exports = {
    CreateAccountController,
    LoginAccountController,
};
