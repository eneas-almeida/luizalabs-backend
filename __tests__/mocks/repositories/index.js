const { AccountsMockRepository } = require('./accounts-mock.repository');
const { FavoritesMockRepository } = require('./favorites-mock.repository');

module.exports = {
    AccountsMockRepository,
    FavoritesMockRepository,
};
